[metadata]
name = onchain_memory
version = 0.0.1

[options]
package_dir =
    = .
packages = find:
python_requires = >=3.10
install_requires =
    web3>=6.11
    eth-account>=0.10
    pydantic>=2.5

[options.packages.find]
where = .
